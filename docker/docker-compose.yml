version: '2.1'
services:

  minthcm-web:
    container_name: minthcm-web
    image: minthcm/minthcm
    
    expose:
      - '80'
    hostname: minthcm-www
    restart: always
    depends_on:
      minthcm-db:
                condition: service_healthy
    tty: true
    stdin_open: true
    env_file:
      - .env
    volumes:
      - minthcm_config:/var/www/html/MintHCM/config
      - minthcm_upload:/var/www/html/MintHCM/legacy/upload
      - minthcm_cron:/var/spool/cron/crontabs
    networks:
      minthcm:

  minthcm-db:
    image: mariadb:10.6 
    container_name: minthcm-db
    restart: always
    networks:
      minthcm:
    environment:
      MYSQL_ROOT_PASSWORD: '${MYSQL_PASSWORD}'
      SYS_NICE: CAP_SYS_NICE
      MYSQL_DEFAULT_AUTHENTICATION_PLUGIN: caching_sha2_password
    healthcheck:
        test: ["CMD", "mysqladmin" ,"ping", "-h", "localhost"]
        timeout: 20s
        retries: 10    
    volumes:
      - minthcm_db:/var/lib/mysql
  
  minthcm-es:
      container_name: minthcm-es
      image: docker.elastic.co/elasticsearch/elasticsearch:7.16.3
      networks:
              minthcm:
      volumes:
          - minthcm_es:/usr/share/elasticsearch/data
      mem_limit: 4g
      environment:
          - node.name=es1
          - cluster.name=minthcm
          - discovery.type=single-node
          - xpack.security.enabled=false       
          - "ES_JAVA_OPTS=-Xms512m -Xmx512m"
      ulimits:
          memlock:
              soft: -1
              hard: -1
          nofile:
              soft: 65536
              hard: 65536
volumes:
  minthcm_db:
  minthcm_es:
  minthcm_config:
  minthcm_config_override:
  minthcm_upload:
  minthcm_custom:
  minthcm_htaccess:
  minthcm_cron:

networks:
  minthcm:
